import SwapList from '@/components/SwapList';

/** My Swaps page — 2-tab listing (Creator / Recipient) */
export default function MySwaps() {
  return (
    <main>
      <SwapList />
    </main>
  );
}
