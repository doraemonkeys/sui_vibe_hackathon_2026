import EscrowList from '@/components/EscrowList';

/** My Escrows page — 3-tab listing (Creator / Recipient / Arbiter) */
export default function MyEscrows() {
  return (
    <main>
      <EscrowList />
    </main>
  );
}
