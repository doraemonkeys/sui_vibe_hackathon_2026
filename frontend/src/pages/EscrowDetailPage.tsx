import EscrowDetail from '@/components/EscrowDetail';

/** Escrow detail page — accessed via /escrow/:id */
export default function EscrowDetailPage() {
  return (
    <main>
      <EscrowDetail />
    </main>
  );
}
