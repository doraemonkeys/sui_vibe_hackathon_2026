import type { ChainEvent } from '@/types';

/** Vertical event timeline — reused in EscrowDetail and SwapDetail */
export default function EventTimeline({ events: _events }: { events: ChainEvent[] }) {
  return <div>EventTimeline</div>;
}
